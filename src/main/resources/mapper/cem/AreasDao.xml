<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.cem.modules.cem.dao.AreasDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="io.cem.modules.cem.entity.AreasEntity" id="areasMap">
        <result property="id" column="id"/>
        <result property="areaid" column="areaid"/>
        <result property="area" column="area"/>
        <result property="cityid" column="cityid"/>
    </resultMap>

	<select id="queryObject" resultType="io.cem.modules.cem.entity.AreasEntity">
		select * from areas where id = #{value}
	</select>

	<select id="queryList" resultType="io.cem.modules.cem.entity.AreasEntity">
		select * from areas
        <choose>
            <when test="sidx != null and sidx.trim() != ''">
                order by ${sidx} ${order}
            </when>
			<otherwise>
                order by id desc
			</otherwise>
        </choose>
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>
	
 	<select id="queryTotal" resultType="int">
		select count(*) from areas 
	</select>
	 
	<insert id="save" parameterType="io.cem.modules.cem.entity.AreasEntity" useGeneratedKeys="true" keyProperty="id">
		insert into areas
		(
			`areaid`, 
			`area`, 
			`cityid`
		)
		values
		(
			#{areaid}, 
			#{area}, 
			#{cityid}
		)
	</insert>
	 
	<update id="update" parameterType="io.cem.modules.cem.entity.AreasEntity">
		update areas 
		<set>
			<if test="areaid != null">`areaid` = #{areaid}, </if>
			<if test="area != null">`area` = #{area}, </if>
			<if test="cityid != null">`cityid` = #{cityid}</if>
		</set>
		where id = #{id}
	</update>
	
	<delete id="delete">
		delete from areas where id = #{value}
	</delete>
	
	<delete id="deleteBatch">
		delete from areas where id in 
		<foreach item="id" collection="array" open="(" separator="," close=")">
			#{id}
		</foreach>
	</delete>

</mapper>